{% extends 'base.html.twig' %}

{% block title %}Edit Joueur{% endblock %}

{% block body %}
    <h1>Modifier le Joueur {{ joueur.nom }}</h1>

    {{ include('joueur/_form.html.twig', {'button_label': 'Modifier'}) }}

    {% if is_granted('ROLE_OFFICIER') %}
        <a href="{{ path('app_joueur_index') }}" class="btn btn-primary">Retour à la liste</a>
        {{ include('joueur/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
