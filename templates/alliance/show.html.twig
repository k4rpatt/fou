{% extends 'base.html.twig' %}

{% block title %}Alliance{% endblock %}

{% block body %}
    <h1>Alliance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alliance.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ alliance.nom }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ alliance.couleur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alliance_index') }}">back to list</a>

    <a href="{{ path('app_alliance_edit', {'id': alliance.id}) }}">edit</a>

    {{ include('alliance/_delete_form.html.twig') }}
{% endblock %}
