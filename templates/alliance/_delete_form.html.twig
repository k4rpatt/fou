<form method="post" action="{{ path('app_alliance_delete', {'id': alliance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alliance.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
