{% extends 'base.html.twig' %}

{% block title %}Progression{% endblock %}

{% block body %}
    <h1>Progression</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ progression.id }}</td>
            </tr>
            <tr>
                <th>DateProgression</th>
                <td>{{ progression.dateProgression ? progression.dateProgression|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PC_tank</th>
                <td>{{ progression.PCTank }}</td>
            </tr>
            <tr>
                <th>PC_avion</th>
                <td>{{ progression.PCAvion }}</td>
            </tr>
            <tr>
                <th>PC_missile</th>
                <td>{{ progression.PCMissile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_progression_index') }}">back to list</a>

    <a href="{{ path('app_progression_edit', {'id': progression.id}) }}">edit</a>

    {{ include('progression/_delete_form.html.twig') }}
{% endblock %}
