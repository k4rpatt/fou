{{ form_start(form,{'attr': {'id': 'form_position_ajout'}}) }}
{#    {{ form_widget(form) }}#}
    {{ form_errors(form) }}
    <div class="row">
        <div class="col">
            <h3>Les infos</h3>
            {{ form_row(form.posX) }}
            {{ form_row(form.posY) }}
            {{ form_row(form.duree) }}
            {{ form_row(form.alliance) }}
        </div>
        <div class="col">
            <h3>Suivi de l'activité</h3>
            {{ form_row(form.ajout) }}
            {{ form_row(form.maj) }}
            {{ form_row(form.suppr) }}
{#            <button class="btn btn-xs btn-success">{{ button_label|default('Mise à jour proprio') }}</button>#}
        </div>
        <div class="col">
            <h3>Stratégie</h3>
            {{ form_rest(form) }}
        </div>
    </div>


{{ form_end(form) }}
